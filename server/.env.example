# Optional: ContextSwitch uses deterministic local reasoning when this is unset.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Local defaults; Render overrides DATABASE_PATH to its persistent disk.
PORT=4000
DATABASE_PATH=./data/contextswitch.db
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
